const React = require('react');
const { Fragment } = require('react');
const { Component } = React;

const App = () => {
    return (
        <>
            <div>탑메뉴가 존재할 위치입니다.</div>
            {/* 작성할 컴포넌트들  */}
        </>
    )
    
}